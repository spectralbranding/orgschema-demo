name: Fleet CI

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quality:
    name: Code Quality & Security
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Python tooling - only if pyproject.toml exists
      - name: Install uv
        if: hashFiles('pyproject.toml') != ''
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
          ignore-nothing-to-cache: true

      - name: Set up Python
        if: hashFiles('pyproject.toml') != ''
        run: uv python install 3.12

      - name: Install Dependencies
        if: hashFiles('pyproject.toml') != ''
        run: uv sync --all-extras --dev

      - name: 🎨 Lint (Black)
        if: hashFiles('pyproject.toml') != ''
        run: uv run black --check .

      - name: ⚖️ Lint (Flake8)
        if: hashFiles('pyproject.toml') != ''
        run: uv run flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics

      - name: 🔍 Type Check (MyPy)
        if: hashFiles('pyproject.toml') != ''
        run: |
          # Check for Python files before running MyPy
          if [ -d "src" ] && find src -name "*.py" -type f | grep -q .; then
             uv run mypy src/ --ignore-missing-imports --exclude "archive|ARCHIVE"
          elif find . -name "*.py" -type f -not -path "./.venv/*" -not -path "./venv/*" -not -path "./archive/*" -not -path "./ARCHIVE/*" -not -path "./node_modules/*" | grep -q .; then
             uv run mypy . --ignore-missing-imports --exclude "tests|archive|ARCHIVE|.venv|venv|node_modules"
          else
             echo "No Python files found, skipping MyPy."
          fi

      - name: 🧪 Test (Pytest)
        if: hashFiles('pyproject.toml') != ''
        run: |
          # Skip if tests require local environment (marker file)
          if [ -f ".ci-skip-tests" ]; then
            echo "Tests skipped (.ci-skip-tests marker found)"
          elif [ -d "tests" ] || [ -f "pytest.ini" ]; then
            uv run pytest
          else
            echo "No tests found, skipping."
          fi

      # Security scan - runs for ALL project types
      - name: Security (Trivy)
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-ref: '.'
          exit-code: '1'
          ignore-unfixed: true
          severity: 'CRITICAL,HIGH'

  tdd-cascade-validation:
    name: TDD Cascade Validation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout demo
        uses: actions/checkout@v4

      - name: Checkout framework (validator)
        uses: actions/checkout@v4
        with:
          repository: spectralbranding/orgschema-framework
          path: _framework

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Set up Python
        run: uv python install 3.12

      - name: Install validator
        working-directory: _framework
        run: uv sync

      - name: Level 1 - Schema validation
        run: |
          cd _framework
          uv run python -m orgschema_framework.validate $GITHUB_WORKSPACE

      - name: Level 2 - Cross-reference integrity
        run: echo "Checked in Level 1 combined run"

      - name: Level 4 - Signal coverage
        run: echo "Checked in Level 1 combined run"

      - name: Level 5 - Experience traceability
        run: echo "Checked in Level 1 combined run"
